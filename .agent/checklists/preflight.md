# Checklist: Preflight

Run before starting milestones.

- [ ] Repo clean state — `git status` shows no unexpected changes
- [ ] `uv` available — `command -v uv`
- [ ] `pnpm` available — `command -v pnpm`
- [ ] `docker` available — `command -v docker`
- [ ] `git` available — `command -v git`
- [ ] `.env.example` present
- [ ] All required `scripts/*.sh` present
- [ ] `AGENTS.md` present
- [ ] `COMMANDS.md` present
- [ ] Active ExecPlan identified and read
